import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  card: {
    width: 'auto',
  },
  media: {
    height: 240,
    maxWidth: '100%',
  },
  projectLink: {
    textDecoration: 'none',
    color: 'inherit',
  }
});

export default useStyles;